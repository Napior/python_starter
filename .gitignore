# Python
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
dist/
build/

# Virtual env — do not commit
.venv/

# Env — never commit
.env
*.env.local

# Data
data/*.db
data/cmdb.json

# requirements.txt is the lockfile — commit it to the repo

# Node / Vite
node_modules/
ui/dist/
ui/.vite/

# OS
.DS_Store
